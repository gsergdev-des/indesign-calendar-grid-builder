# Календарная сетка для InDesign — RU / ZH

Скрипт для InDesign, который строит календарную сетку на любой год.
Эта ветка — русская и китайская: интерфейс на русском или 中文, календарь
русско-английский или китайско-английский.

[中文说明 → README.zh-CN.md](README.zh-CN.md)

Украинская ветка живёт отдельно и сюда не входит.

## Вёрстка календарной сетки — та задача, где скучно и одновременно страшно ошибиться

Скучно, потому что это 12 одинаковых страниц. Страшно, потому что ошибку в одной
дате найдёт не корректор, а клиент — уже после печати тиража.

Поэтому скрипт считает сетку сам.

### Что внутри

В сетке на 2027 год первая неделя января — 53-я, потому что принадлежит ещё
2026 году. Такие вещи руками не считают, их либо знают, либо ошибаются.

→ **Два независимых языка.** «Язык интерфейса» — русский или 中文. «Язык
календаря» — «Русский + English» или «中文 + English». Их можно смешивать:
русский интерфейс и китайская сетка — обычный случай в типографии.

→ **Три готовых шаблона раскладки:** мини-календари соседних месяцев под
сеткой, рядом с ней или столбиком справа. Переключаются одной радиокнопкой,
каждый со своим набором кеглей и отступов.

→ **Диалог настроек при запуске:** формат листа, поля, доля ширины под сетку,
шрифт из списка установленных в системе, кегли, цвета. Плюс живая схема
страницы, которая перерисовывается, пока вы меняете цифры.

→ **Проверка геометрии перед созданием документа.** Скрипт просчитывает все
12 месяцев и предупреждает конкретикой: «Блок выходит за нижнее поле на 4.8 мм
(Май)».

Текст не «вшит» во фреймы: скрипт создаёт группу стилей абзацев, поэтому кегли,
шрифт и цвет потом меняются глобально, без перезапуска.

12 страниц A2 — за несколько секунд и в один шаг отмены.

## Китайская сетка

Названия месяцев — 一月 … 十二月, дни недели — 星期一 … 星期日, сокращённые в
мини-календарях — 一 二 三 四 五 六 日. Нижняя строка шапки остаётся английской
(Monday … Sunday), как и в русском варианте.

**Два шрифта.** Цифры, латиница и кириллица набираются основным шрифтом,
иероглифы — отдельным китайским. Так крупные числа в сетке (52 pt в шаблоне
«мини под сеткой») не достаются от SimSun, где они выглядят вяло.

В диалоге при китайском календаре появляется вторая пара полей — «Шрифт
иероглифов» и его начертания. Опознанные китайские семейства идут в списке
первыми, до разделителя.

В документе иероглифы размечены знаковым стилем **CJK** в группе `CAL` на
панели Character Styles. В нём задан только шрифт — кегль, цвет и выключка
наследуются от стиля абзаца. Поменяете шрифт в этом стиле — перерисуются все
иероглифы во всех 12 месяцах, цифры останутся прежними.

## Системные требования

Кроме самого InDesign не нужно ничего: ни плагинов, ни расширений, ни
интернета. Но нужно до 30 сек. на поиск шрифтов при запуске.

## Шрифты

Это единственная внешняя зависимость, которая действительно влияет на
результат.

**Для русского календаря** нужен хотя бы один шрифт с кириллицей и курсивным
начертанием. Скрипт ищет первый доступный из списка:

    Myriad Pro → Segoe UI → Roboto → Arial → Helvetica

**Для китайского** дополнительно нужен шрифт с иероглифами. Скрипт ищет первый
доступный из списка:

    Source Han Sans SC → Noto Sans SC → Noto Sans CJK SC
    → Source Han Serif SC → Noto Serif SC
    → Microsoft YaHei (微软雅黑) → PingFang SC → Hiragino Sans GB
    → Heiti SC → SimHei (黑体) → SimSun (宋体)
    → Adobe Song Std → Adobe Heiti Std

Свой шрифт можно выбрать прямо в диалоге: список показывает то, что реально
установлено в системе.

Проверить покрытие глифов из скрипта InDesign не позволяет, поэтому опознание
китайских шрифтов идёт по названию семейства. Если выбранный шрифт на CJK не
похож, скрипт предупредит перед созданием документа — но всё равно построит.
Итог видно сразу: подменённые глифы InDesign подсвечивает розовым.

## Файл настроек

Сохраняет параметры между запусками, включая оба выбранных языка:

- Windows: `%APPDATA%\CalendarGridBuilderML.txt`
- macOS: `~/Library/Application Support/CalendarGridBuilderML.txt`

Имя отличается от украинской ветки (`CalendarGridBuilder.txt`) — настройки двух
скриптов не мешают друг другу. Если записать не удастся, скрипт просто
поработает без памяти между сессиями — ошибки не будет.

## Что скрипт делает

1. Создаёт новый документ. Открытые документы не трогает вообще.
2. Не сохраняет файлов, кроме упомянутого текстового файла настроек.
3. Не обращается к сети.
4. Ничего не устанавливает и не меняет настроек InDesign.

Всё построение — один шаг отмены: Ctrl+Z / Cmd+Z убирает всё сразу.

## Установка в InDesign

1. Откройте панель скриптов: **Window → Utilities → Scripts**.
2. В панели Scripts найдите папку **User**.
3. Нажмите на папку User правой кнопкой мыши и выберите
   **Reveal in Explorer** (Windows) или **Reveal in Finder** (macOS).
   Откроется папка, в которой InDesign хранит пользовательские скрипты.
4. Откройте папку **Scripts Panel** и скопируйте в неё файл
   `calendargridbuilder_ru_zh_v1_0.jsx`.
5. Вернитесь в InDesign. В панели Scripts откройте **User → Scripts Panel**.
6. Дважды щёлкните по файлу, чтобы запустить скрипт.

Перезапускать InDesign не нужно — файл подхватывается сразу.
