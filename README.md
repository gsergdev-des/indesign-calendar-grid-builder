# indesign-calendar-grid-builder
Cкрипт для InDesign, який будує сітку на будь-який рік
<img width="1234" height="869" alt="Screenshot_4" src="https://github.com/user-attachments/assets/6a91c8be-ff7a-4efa-91c3-a06db9e852fe" />

## Верстка календарної сітки — це та задача, де нудно і водночас страшно помилитися

Нудно, бо це 12 однакових сторінок. Страшно, бо помилку в одній даті знайде не коректор, а клієнт — уже після друку тиражу.

Тому я зробив скрипт для InDesign, який будує сітку на будь-який рік сам.

### Що всередині:

У сітці на 2027 рік перший тиждень січня — 53-й, бо він належить ще 2026 року. Такі речі руками не рахують, їх або знають, або помиляються.

→ Три готові шаблони розкладки: міні-календарі сусідніх місяців під сіткою, поруч із нею або стовпчиком праворуч. Перемикаються однією радіокнопкою, кожен зі своїм набором кеглів і відступів.

→ Діалог налаштувань при запуску: формат аркуша, поля, частка ширини під сітку, шрифт зі списку встановлених у системі, кеглі, кольори. Плюс жива схема сторінки, яка перемальовується, поки ви змінюєте цифри.

→ Перевірка геометрії перед створенням документа. Скрипт прораховує всі 12 місяців і попереджає конкретикою: «блок виходить за нижнє поле на 4.8 мм (Травень)».

Текст не «вшитий» у фрейми: скрипт створює групу стилів абзаців, тож кеглі, шрифт і колір потім міняються глобально, без перезапуску.

12 сторінок A2 — за кілька секунд і в один крок скасування.

### Системні вимоги. 
Крім самого InDesign не потрібно нічого, ні плагінів, ні розширень, ні інтернету,
Але потребує 30 сек. для пошуку шрифтів при запуску.

### Шрифти. 
Це єдина зовнішня залежність, яка справді впливає на результат.
Потрібен щонайменше один шрифт із кирилицею та курсивним накресленням.
Скрипт шукає перший доступний із переліку:
Myriad Pro → Segoe UI → Roboto → Arial → Helvetica
Свій шрифт можна обрати просто в діалозі: список показує те, що реально встановлено
в системі.

### Файл налаштувань -  (зберігає параметри між запусками):

Windows: `%APPDATA%\CalendarGridBuilder.txt` 
macOS: `~/Library/Application Support/CalendarGridBuilder.txt` 

Якщо записати туди не вдасться, скрипт просто працюватиме без пам'яті
між сесіями — помилки не буде.

### Що скрипт робить
 1. Створює новий документ. Відкриті документи не чіпає взагалі.
 2. Не зберігає файлів, окрім згаданого текстового файла налаштувань.
 3. Не звертається до мережі.
 3. Не встановлює нічого і не змінює налаштувань InDesign.
 Уся побудова — один крок скасування: Ctrl+Z / Cmd+Z прибирає  все одразу.
  
### Встановлення в InDesign.
1. Відкрийте панель скриптів у InDesign:
   Window → Utilities → Scripts.
2. У панелі Scripts знайдіть папку User.
3. Натисніть на папку User правою кнопкою миші та виберіть:
   Reveal in Explorer — для Windows;
   Reveal in Finder — для macOS.
   Відкриється папка, у якій InDesign зберігає користувацькі скрипти.
4. Відкрийте папку Scripts Panel та скопіюйте в неї файл `calendargridbuilder_v2_2_betta.jsx`.
5. Поверніться до InDesign. У панелі Scripts відкрийте User → Scripts Panel.
6. Двічі клацніть файл `calendargridbuilder_v2_2_betta.jsx`, щоб запустити скрипт.

Перезапускати InDesign не потрібно — файл підхоплюється одразу.
