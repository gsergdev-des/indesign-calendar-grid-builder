# InDesign 日历网格生成器 — 俄文 / 中文

用于 Adobe InDesign 的脚本，可为任意年份生成日历网格。
本分支为俄文与中文版本：界面可选俄文或中文，日历可选「俄文 + 英文」或
「中文 + 英文」。
<img width="1202" height="1018" alt="Screenshot_1" src="https://github.com/user-attachments/assets/87888cc4-4e4c-4d25-9fff-8cb3e160f669" />

[Русское описание → README.md](README.md)

乌克兰文版本是独立分支，不包含在此。

## 排一套日历网格，既枯燥又容易出事

枯燥，是因为要做 12 个几乎一样的页面。容易出事，是因为某一个日期排错了，
发现的人往往不是校对，而是客户 —— 而那时整批已经印完了。

所以让脚本自己算。

### 功能

2027 年的网格里，一月的第一周是第 53 周，因为它仍属于 2026 年。
这类事情没人靠手算，要么知道，要么出错。

→ **两个互相独立的语言选项。**「界面语言」为俄文或中文；「日历语言」为
「Русский + English」或「中文 + English」。两者可以自由组合 —— 用俄文界面
排中文日历，在印厂是常见情形。

→ **三种现成版式：** 相邻月份的小月历放在网格下方、网格右侧，或右侧竖排。
一个单选按钮即可切换，每种版式都有自己的字号与间距。

→ **启动时的设置对话框：** 页面尺寸、页边距、网格宽度占比、系统已装字体、
字号、颜色。右侧还有实时页面示意图，改数字时同步重绘。

→ **生成前的版面检查。** 脚本会算完全部 12 个月，并给出具体提示：
「版块超出下页边距 4.8 毫米（五月）」。

文字不是「焊死」在框架里的：脚本会建立段落样式组，之后字号、字体和颜色都能
全局修改，无需重新运行脚本。

12 页 A2，几秒钟生成，一次撤销即可全部还原。

## 中文日历

月份为 一月 … 十二月，星期为 星期一 … 星期日，小月历中的缩写为
一 二 三 四 五 六 日。表头下方一行保持英文（Monday … Sunday），与俄文版一致。

**两套字体。** 数字、拉丁字母和西里尔字母使用正文字体，汉字使用单独的中文
字体。这样网格中的大号数字（「小月历在下方」版式为 52 pt）就不必取自宋体 ——
那样会显得松散无力。

选择中文日历时，对话框中会出现第二组字体设置：「中文字体」及其字重。
已识别为中文的字体族排在列表前列，分隔线之后才是其余字体。

在生成的文档中，汉字使用「字符样式」面板 `CAL` 样式组里的 **CJK** 样式。
该样式只指定字体，字号、颜色和对齐方式仍继承自段落样式。修改该样式中的字体，
12 个月里的全部汉字都会随之改变，而数字保持不变。

## 系统要求

除 InDesign 本身外无需任何东西：不需要插件、扩展或网络连接。
启动时最多需要 30 秒来枚举系统字体。

## 字体

这是唯一真正影响结果的外部依赖。

**俄文日历** 至少需要一款包含西里尔字母且带斜体的字体。脚本按以下顺序取第一款
可用的：

    Myriad Pro → Segoe UI → Roboto → Arial → Helvetica

**中文日历** 还需要一款含汉字的字体。脚本按以下顺序取第一款可用的：

    Source Han Sans SC → Noto Sans SC → Noto Sans CJK SC
    → Source Han Serif SC → Noto Serif SC
    → 微软雅黑 (Microsoft YaHei) → PingFang SC → Hiragino Sans GB
    → Heiti SC → 黑体 (SimHei) → 宋体 (SimSun)
    → Adobe Song Std → Adobe Heiti Std

也可以直接在对话框中指定字体：列表显示的就是系统中实际安装的字体。

InDesign 的脚本接口无法检测某款字体是否真的包含所需字形，因此中文字体是按
字体族名称识别的。若所选字体看起来不像中文字体，脚本会在生成前提示，但仍会
继续生成。结果一眼可辨：被替换的字形会被 InDesign 标为粉色高亮。

## 设置文件

在两次运行之间保存参数，包括所选的两种语言：

- Windows：`%APPDATA%\CalendarGridBuilderML.txt`
- macOS：`~/Library/Application Support/CalendarGridBuilderML.txt`

文件名与乌克兰文分支（`CalendarGridBuilder.txt`）不同，两个脚本的设置互不干扰。
若写入失败，脚本只是不再记住上次的设置，不会报错。

## 脚本的行为

1. 新建一个文档。完全不触碰已打开的文档。
2. 除上述文本设置文件外，不保存任何文件。
3. 不访问网络。
4. 不安装任何东西，也不修改 InDesign 的偏好设置。

整个生成过程是一步撤销：Ctrl+Z / Cmd+Z 可一次性全部还原。

## 在 InDesign 中安装

1. 打开脚本面板：**窗口 → 实用程序 → 脚本**
   （Window → Utilities → Scripts）。
2. 在 Scripts 面板中找到 **User** 文件夹。
3. 右键点击 User 文件夹，选择 **Reveal in Explorer**（Windows）或
   **Reveal in Finder**（macOS）。这会打开 InDesign 存放用户脚本的目录。
4. 进入 **Scripts Panel** 文件夹，将 `calendargridbuilder_ru_zh_v1_0.jsx`
   复制进去。
5. 回到 InDesign，在 Scripts 面板中展开 **User → Scripts Panel**。
6. 双击该文件即可运行脚本。

无需重启 InDesign —— 文件会被立即识别。
